-- ============================================================
-- Seed Data
-- Default admin user: admin / Admin@1234
-- Password hash is generated by bcrypt in DatabaseConnection.seed()
-- This file is kept for documentation; actual seeding is done
-- programmatically so bcrypt can hash the password at runtime.
-- ============================================================

-- The INSERT below is intentionally NOT executed directly —
-- DatabaseConnection.seed() runs an equivalent parameterised query.
--
-- INSERT INTO users (username, full_name, email, password_hash, role, is_active)
-- VALUES (
--     'admin',
--     'System Administrator',
--     'admin@taxes.tn',
--     '<bcrypt_hash_of_Admin@1234>',
--     'admin',
--     1
-- );